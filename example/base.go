package main

import "fmt"

func main() {
	arr := make(map[string]string)
	// arr := make([]string, 0)
	// var arr []string
	// arr = append(arr, "a")
	// arr[1] = "b"
	arr["s"] = "b"
	fmt.Println(arr)
}
